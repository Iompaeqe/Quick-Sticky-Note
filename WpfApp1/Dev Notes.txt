setup:
$publishDir = ".\WpfApp1\bin\Release\net9.0-windows\win-x64\publish";
$iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe";

Remove-Item $publishDir -Recurse -Force -ErrorAction SilentlyContinue;
dotnet publish .\WpfApp1\WpfApp1.csproj -c Release -r win-x64 --self-contained false;
& $iscc /FQuickSticky_Setup ".\InstallerScript.iss";

standalone:
$publishDir = ".\WpfApp1\bin\Release\net9.0-windows\win-x64\publish";
$iscc = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe";

Remove-Item $publishDir -Recurse -Force -ErrorAction SilentlyContinue;
dotnet publish .\WpfApp1\WpfApp1.csproj -c Release -r win-x64 --self-contained true;
& $iscc /FQuickSticky_Standalone_Full_Setup ".\InstallerScript.iss";



stored files should have the name of the note in the file name
if the note is empty when it's closed, donot backup.